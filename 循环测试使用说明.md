# 循环测试执行脚本使用说明

## 📋 概述

本目录包含两个循环测试执行脚本，用于自动化执行数字员工创建和对话流程测试。

## 📂 脚本文件

### 1. `loop_test_conversation.py` - 完整版循环测试脚本

**功能**：循环执行完整的测试流程（创建数字员工 + 对话）

**测试流程**：
```
创建数字员工 → 对话 → 新建会话 → 选择技能 → 输入消息 → 发送
```

**特点**：
- ✅ 每次循环创建新的数字员工
- ✅ 完整的测试流程覆盖
- ✅ 详细的统计报告
- ✅ 失败截图保存
- ✅ 支持自定义循环次数

### 2. `simple_loop_conversation.py` - 简化版循环测试脚本

**功能**：对指定数字员工循环执行对话流程（不创建新数字员工）

**测试流程**：
```
对话 → 新建会话 → 选择技能 → 输入消息 → 发送
```

**特点**：
- ✅ 不创建新数字员工，使用现有员工
- ✅ 执行速度更快
- ✅ 适合压力测试和稳定性测试
- ✅ 支持自定义数字员工ID

## 🚀 使用方法

### 完整版脚本使用

```bash
# 进入项目目录
cd d:\qaTest\ui_frame_redClaw

# 默认执行200次
python loop_test_conversation.py

# 自定义执行次数（例如100次）
python loop_test_conversation.py 100

# 自定义执行次数（例如50次）
python loop_test_conversation.py 50
```

### 简化版脚本使用

```bash
# 进入项目目录
cd d:\qaTest\ui_frame_redClaw

# 默认执行200次，使用默认数字员工ID
python simple_loop_conversation.py

# 自定义执行次数（例如100次）
python simple_loop_conversation.py 100

# 自定义执行次数和数字员工ID
python simple_loop_conversation.py 100 "your-agent-id-here"
```

## 📊 输出结果

### 控制台输出

脚本执行过程中会实时显示：

```
================================================================================
🔄 开始执行第 1/200 次测试
================================================================================
[第1次] 创建数字员工: 循环测试_1_20260522_101530
[第1次] 数字员工创建成功 ✅ - ID: 10e8a24f-xxx
[第1次] 执行对话流程...
[第1次] 对话流程完成 ✅
✅ 第 1 次测试成功!

📊 当前进度: 1/200 | 成功率: 100.00%
```

### 统计摘要

执行完成后会显示统计摘要：

```
================================================================================
📊 测试执行统计摘要
================================================================================
总执行次数: 200
成功次数: 198 ✅
失败次数: 2 ❌
成功率: 99.00%
平均耗时: 15.32 秒
总耗时: 3064.00 秒 (51.07 分钟)
================================================================================
```

### 测试报告文件

完整版脚本会生成详细的测试报告文件：

```
reports/loop_test_reports/loop_test_report_20260522_101530.txt
```

报告内容包括：
- 执行时间
- 统计数据
- 每次测试的详细结果
- 失败原因

### 失败截图

测试失败时会自动保存截图：

```
reports/screenshots/loop_test_failure_1_20260522_101530.png
```

## ⚙️ 配置说明

脚本会自动读取以下配置：

### 从 `config/test_config.yaml` 读取：

```yaml
test:
  base_url: "http://10.11.150.76:10088"  # 测试环境URL

test_data:
  users:
    admin:
      username: "admin"    # 登录用户名
      password: "123456"   # 登录密码
```

## 🎯 使用场景

### 场景1：完整功能测试
使用 `loop_test_conversation.py` 进行完整的功能测试：
- 验证数字员工创建功能
- 验证对话流程完整性
- 测试系统整体稳定性

```bash
python loop_test_conversation.py 50
```

### 场景2：对话功能压力测试
使用 `simple_loop_conversation.py` 对对话功能进行压力测试：
- 快速执行大量对话测试
- 不创建新数字员工，减少资源消耗
- 专注于对话功能的稳定性

```bash
python simple_loop_conversation.py 200
```

### 场景3：特定数字员工测试
对特定数字员工进行测试：

```bash
python simple_loop_conversation.py 100 "10e8a24f-1eb8-49d9-9220-a51f5d6d7232"
```

## ⚠️ 注意事项

1. **执行时间**：
   - 完整版每次循环约15-20秒
   - 简化版每次循环约8-12秒
   - 200次完整测试约需50-60分钟

2. **资源消耗**：
   - 完整版会创建大量数字员工，注意清理
   - 简化版资源消耗较少

3. **中断执行**：
   - 按 `Ctrl+C` 可中断执行
   - 中断后会显示当前统计信息

4. **失败处理**：
   - 单次失败不会中断整体执行
   - 失败信息会记录到日志和报告

5. **浏览器窗口**：
   - 执行过程中会显示浏览器窗口
   - 可观察测试执行过程

## 🔧 高级配置

### 修改执行速度

编辑脚本中的 `slow_mo` 参数：

```python
self.browser = self.playwright.chromium.launch(
    headless=False,
    slow_mo=300,  # 修改此值（毫秒）
    ...
)
```

### 修改超时时间

编辑脚本中的超时设置：

```python
self.page.set_default_timeout(30000)  # 30秒
self.page.set_default_navigation_timeout(30000)  # 30秒
```

### 无头模式

修改 `headless` 参数为 `True` 可在无头模式下运行：

```python
self.browser = self.playwright.chromium.launch(
    headless=True,  # 无头模式
    ...
)
```

## 📈 性能优化建议

1. **使用简化版**：如果不需要测试创建功能，使用简化版速度更快
2. **调整等待时间**：根据网络情况调整 `wait_for_timeout` 参数
3. **无头模式**：生产环境测试可使用无头模式提升性能
4. **分批执行**：大量测试可分批执行，避免长时间占用资源

## 🐛 故障排查

### 问题1：登录失败
- 检查 `test_config.yaml` 中的用户名密码
- 检查网络连接和URL配置

### 问题2：元素定位失败
- 检查页面是否正常加载
- 查看失败截图分析原因
- 可能需要更新Page Object定位器

### 问题3：超时错误
- 增加超时时间设置
- 检查网络延迟
- 检查服务器响应速度

## 📞 技术支持

如有问题，请查看：
- 日志文件：`logs/` 目录
- 失败截图：`reports/screenshots/` 目录
- 测试报告：`reports/loop_test_reports/` 目录
