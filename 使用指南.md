# Python + Playwright 自动化 UI 测试框架使用指南

## 🎉 恭喜！框架搭建完成

您的自动化 UI 测试框架已经成功搭建并验证完成！这是一个功能完整、易于扩展的现代化测试框架。

## 📁 项目结构说明

```
ui_frame/
├── pages/                    # 页面对象层
│   ├── __init__.py
│   ├── base_page.py         # 基础页面类
│   └── login_page.py        # 登录页面示例
├── tests/                   # 测试用例层
│   ├── __init__.py
│   ├── test_login.py        # 登录测试用例
│   └── test_demo.py         # 演示测试用例
├── utils/                   # 工具类层
│   ├── __init__.py
│   ├── logger.py            # 日志工具
│   ├── config_reader.py     # 配置读取
│   ├── page_utils.py        # 页面操作工具
│   └── data_manager.py      # 数据管理工具
├── config/                  # 配置文件
│   └── test_config.yaml     # 测试配置
├── test_data/              # 测试数据
│   └── login_data.json      # 测试数据文件
├── reports/                # 测试报告（自动生成）
├── logs/                   # 日志文件（自动生成）
├── conftest.py             # pytest全局配置
├── pytest.ini             # pytest配置
├── run_tests.py            # 自定义测试运行脚本
├── setup.py                # 完整安装脚本
├── quick_setup.py          # 快速安装脚本
└── README.md               # 项目文档
```

## 🚀 快速开始

### 1. 运行演示测试

```bash
# 运行演示测试
python -m pytest tests/test_demo.py -v

# 使用自定义脚本运行
python run_tests.py --tests tests/test_demo.py --verbose
```

### 2. 运行冒烟测试

```bash
# 运行所有冒烟测试
python run_tests.py --markers smoke --html-report

# 运行所有UI测试
python run_tests.py --markers ui --html-report
```

### 3. 生成测试报告

```bash
# 生成HTML报告
python run_tests.py --tests tests/ --html-report

# 查看报告位置
# reports/report.html
```

## 🛠️ 常用操作

### 创建新的页面对象

1. 在 `pages/` 目录下创建页面类文件
2. 继承 `BasePage` 类
3. 实现 `navigate()` 和 `is_loaded()` 方法

```python
from pages.base_page import BasePage
from playwright.sync_api import Page

class YourPage(BasePage):
    # 定义页面元素
    ELEMENT_SELECTOR = "#your-element"

    def __init__(self, page: Page):
        super().__init__(page)
        self.page_url = f"{self.base_url}/your-page"

    def navigate(self):
        self.page.goto(self.page_url)
        self.wait_for_page_load()

    def is_loaded(self) -> bool:
        return self.is_element_visible(self.ELEMENT_SELECTOR)
```

### 创建新的测试用例

1. 在 `tests/` 目录下创建测试文件
2. 使用 pytest 的标记系统分类测试

```python
import pytest
from pages.your_page import YourPage

class TestYourFeature:
    @pytest.fixture(autouse=True)
    def setup(self, page):
        self.page = page
        self.your_page = YourPage(page)

    @pytest.mark.smoke
    @pytest.mark.ui
    def test_your_feature(self):
        self.your_page.navigate()
        assert self.your_page.is_loaded()
        # 执行测试步骤
```

### 使用测试数据

1. 在 `test_data/` 目录下创建数据文件（JSON/YAML/CSV/Excel）
2. 使用 `DataManager` 加载数据

```python
from utils.data_manager import DataManager

# 加载测试数据
test_data = DataManager.get_test_data("your_data.json")

# 参数化测试
@pytest.mark.parametrize("data", test_data)
def test_with_data(self, data):
    # 使用数据执行测试
    pass
```

## 🎯 测试执行方式

### 基本执行

```bash
# 运行所有测试
pytest

# 运行指定文件
pytest tests/test_login.py

# 运行指定标记
pytest -m smoke
```

### 使用自定义脚本

```bash
# 查看所有选项
python run_tests.py --help

# 指定浏览器
python run_tests.py --browser firefox

# 并行执行
python run_tests.py --parallel 4

# 生成报告
python run_tests.py --html-report --allure

# 调试模式
python run_tests.py --debug --headed --slowmo 1000
```

## 📊 报告查看

### HTML 报告

- 位置: `reports/report.html`
- 包含测试结果、截图、执行时间等信息

### 控制台输出

- 实时显示测试执行进度
- 显示详细的日志信息

### 日志文件

- 位置: `logs/test_YYYYMMDD.log`
- 包含详细的执行日志

## ⚙️ 配置说明

### 环境变量 (.env)

```
BASE_URL=https://your-test-site.com
TEST_ENV=dev
BROWSER_HEADLESS=false
BROWSER_SLOW_MO=100
```

### 测试配置 (config/test_config.yaml)

```yaml
test:
  base_url: "https://your-site.com"
  environment: "dev"

browser:
  headless: false
  timeout: 30000
```

## 🏷️ 测试标记

- `@pytest.mark.smoke`: 冒烟测试
- `@pytest.mark.regression`: 回归测试
- `@pytest.mark.critical`: 关键功能测试
- `@pytest.mark.ui`: UI 界面测试
- `@pytest.mark.slow`: 慢速测试

## 🐛 调试技巧

### 1. 开启浏览器显示

```bash
python run_tests.py --headed
```

### 2. 减慢执行速度

```bash
python run_tests.py --slowmo 1000
```

### 3. 暂停调试

```python
# 在代码中添加
page.pause()
```

### 4. 查看详细日志

```bash
python run_tests.py --verbose --debug
```

## 📈 扩展建议

1. **添加更多页面对象**: 根据被测应用创建对应的页面类
2. **丰富测试数据**: 添加更多测试场景的数据文件
3. **集成 CI/CD**: 在持续集成流水线中运行测试
4. **添加 API 测试**: 结合 requests 库进行接口测试
5. **性能测试**: 集成性能监控和分析

## 🆘 常见问题

### Q: 浏览器启动失败？

A: 运行 `playwright install` 重新安装浏览器

### Q: 依赖包安装失败？

A: 运行 `python quick_setup.py` 重新安装

### Q: 测试运行缓慢？

A: 使用 `--parallel` 参数并行执行

### Q: 如何查看测试报告？

A: 打开 `reports/report.html` 文件查看详细报告

## 🎊 恭喜！

您现在拥有了一个功能完整的自动化测试框架！可以开始编写您的测试用例了。

如有问题，请查看 README.md 或提交 Issue。祝您测试愉快！ 🚀
